<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.scoula.lh.att.mapper.DanziAttMapper">
    <insert id="create" parameterType="org.scoula.lh.att.domain.DanziAttVO">
        INSERT INTO danzi_att_table (danzi_id, fl_ds_cd_nm, cmn_ahfl_nm, ahfl_url)
        VALUES (#{danziId}, #{flDsCdNm}, #{cmnAhflNm}, #{ahflUrl})
    </insert>

    <insert id="createAll" parameterType="java.util.List">
        INSERT INTO danzi_att_table (danzi_id, fl_ds_cd_nm, cmn_ahfl_nm, ahfl_url)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.danziId}, #{item.flDsCdNm}, #{item.cmnAhflNm}, #{item.ahflUrl})
        </foreach>
    </insert>

    <!-- 단지 ID로 조회 -->
    <select id="getBydanziId" parameterType="int" resultType="org.scoula.lh.att.domain.DanziAttVO">
        SELECT danzi_id, fl_ds_cd_nm, cmn_ahfl_nm, ahfl_url
        FROM danzi_att_table
        WHERE danzi_id = #{danziId}
    </select>
</mapper>